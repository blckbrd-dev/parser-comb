
run `sbt`, and then try `compile` to download the compiler and check status (should always be okay to compile something pushed to master), and then run `test` to test the combinator library. Not all code is there (`???` are present in code), so the tests won't all pass automatically.

